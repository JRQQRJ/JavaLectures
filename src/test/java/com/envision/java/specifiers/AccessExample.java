package com.envision.java.specifiers;

public class AccessExample {

    public static void main(String[] args) {

        Class3StudentsAccessSpecifiers test = new Class3StudentsAccessSpecifiers();
       // test. --> we can see a, c,c d as all belong inside the same package, protected an default variables are -
       // -visible inside the same package
        /*

        And a is public, aslo getValueOfA is visible as it's a public method

         */
    }
}